The automatic import of ...
%SYSTEM%
... from CloudArea %CLOUDAREA% was rejected.

The reson for this reject is, the CloudArea has not been activated. On CloudAreas with this CI-Status no deployments are allowed. 

As databoss (or write authorized person) of %APPL%, you can change the CI-Status to installed/activ, if you wan't to enable/allow deploment operations on the CloudArea.
%URL%

<b><font color=red>Deployments on not activated CloudAreas causes in security issues!</font></b>
